# Services Layer

This folder contains API service modules that communicate with your backend endpoints on the separate PC.

## Purpose
- Encapsulate all HTTP requests to backend APIs
- Handle authentication, error handling, and request/response transformation
- Keep business logic separate from UI components